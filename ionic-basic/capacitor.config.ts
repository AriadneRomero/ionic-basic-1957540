import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.fcfm.app',
  appName: 'ionic-basic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
